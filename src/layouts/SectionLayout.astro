---
import BaseLayout from "./BaseLayout.astro";

const { title } = Astro.props;
---

<BaseLayout title={title}>
  <!-- Sub pages: header 바로 아래에서 본문 시작 (배너/중간목차/왼쪽목차 제거) -->
  <div class="section-layout">
    <section class="sub-content">
      <slot />
    </section>
  </div>
</BaseLayout>
